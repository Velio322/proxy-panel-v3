{$PANEL_DOMAIN} {
    # Client SPA
    handle /* {
        reverse_proxy client:80
    }

    # API
    handle /api/* {
        reverse_proxy server:3000
    }

    # Health check
    handle /health {
        reverse_proxy server:3000
    }
}
